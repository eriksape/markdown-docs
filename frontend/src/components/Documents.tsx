import React, { FunctionComponent } from 'react';

interface DocumentsProps {}

const Documents: FunctionComponent<DocumentsProps> = ({}) => (
  <div className="Documents">Documents list</div>
);

export default Documents;
